% TEST_PRESERVES_PLOTINFO override because ssm objects don't do anything
% with plotinfo so far.
function res = test_preserves_plotinfo(varargin)  
  % Return result message
  res = 'Check of plotinfo is skipped.';
end
% END