*.log
# Root-level XML artifacts from test runs (ao XML save/load tests).
# Subdirectory XML files (toolbox metadata, help, prefs templates) are NOT ignored.
/*.xml
# Release zip files built by make_release.sh
/*.zip
/.claude
